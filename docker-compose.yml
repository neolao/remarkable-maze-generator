services:
  web:
    build: .
    image: remarkable-maze-generator-web
    ports:
      - "4367:4367"
    environment:
      HOST: 0.0.0.0
      PORT: 4367
    volumes:
      - maze-credentials:/home/node/.config/remarkable-maze-generator
    restart: unless-stopped

volumes:
  maze-credentials:
