# remarkable-maze-generator — Codebase index
> Generated by /vibe:sync on 2026-07-12 (incremental). Re-run to update.

## Modules
- [`modules/core.md`](modules/core.md) — shared core (maze generation pluggable across 4 selectable algorithms — growing-tree (default, difficulty-tunable, only one supporting bridge crossings), kruskal, wilson, aldous-broder — each isolated in its own module, and a choice of maze type — classic thin-wall `rectangle`, thick curvy-path `rectangle-crossing`, or `circle` (a real growing-sector topology in its own `circle-maze/` module tree, entirely separate from the rectangular grid — more sectors per ring further out, always an exact multiple of the ring inside it so cells line up cleanly ring to ring, with its own generation, solver, and polar renderer), an optional path-length target (short/medium/long) that generates several candidate seeds (a user-configurable count, capped at 50) and keeps the best match, solution path and branch-point detection, PDF and SVG rendering sharing common wall/path-geometry layers, SVG preview with an optional solution/branch-point overlay, generation-parameters footer, shared solution-mode/maze-type/algorithm/path-length validation, reMarkable Cloud auth + upload via `rmapi-js`, verified against a real account)
- [`modules/cli.md`](modules/cli.md) — CLI (Commander) consuming `core` — `generate`, `send`, and combined `generate-and-send` commands implemented, with `--solution`, `--type`, `--algorithm`, `--path-length` and `--path-length-candidates` options on `generate`/`generate-and-send`
- [`modules/web.md`](modules/web.md) — web server (Fastify) consuming `core` — a maze configuration form (including a maze type dropdown, a generation algorithm dropdown, a solution display mode dropdown for the PDF, a path length dropdown, a path-length candidate count field, and a "show solution on preview" checkbox with a branch-point count display) with SVG image preview, PDF download, and direct send-to-reMarkable (with guided pairing and an optional target reMarkable Cloud folder) all implemented, with a dedicated responsive stylesheet applied to the page, and the form's last-used values remembered via a cookie and pre-filled on the next visit; `@fastify/helmet` security headers, and binds to `0.0.0.0` by default (override via `HOST`) — no auth layer, so anyone on the local network can reach it

## Observed patterns
- `core`'s domain vocabulary (`Maze`, `Cell`, `MazeType`, `MazeAlgorithm`, `PathLengthTarget`, `GenerateMazeOptions`, and their validation rules) is isolated in `maze-domain.ts`, depended on by generation/rendering code rather than the reverse, establishing the domain boundary noted as missing in `CLAUDE.md`'s `vibe:review-ddd` entry (see ADR 050)
- Per-`MazeType`/`MazeAlgorithm` dispatch in `core` is centralized in `Record`-keyed strategy/registry maps (`maze-algorithm-registry.ts`, `maze-render-strategy.ts`, and a solver strategy map in `maze-solver.ts`) instead of duplicated conditionals, so TypeScript's exhaustiveness checking forces every new type/algorithm to be registered in one place per concern (see ADR 049)
- npm workspaces monorepo (`packages/*`), one `package.json` per package with `private: true`
- `core` resolves to its TypeScript sources for dev/test (via a `development` package export condition, no build required) and to its compiled `dist/` output for everything else, including the production web server and the packaged CLI binary (see ADR 044); a `postinstall` hook applies a `patch-package` fix for a broken ESM import in `rmapi-js` (see ADR 045)
- A Vitest test file next to each source module (`*.test.ts`)
- Style and lint centralized via Biome at the root (`biome.json`), tab formatting, double quotes
- Requires Node.js 25+ (pinned in `.nvmrc`, declared in the root `package.json` `engines` field) — needed by `rmapi-js`'s use of native `Uint8Array` base64/hex methods

## Other context files
- [`models.md`](models.md) — data models
- [`glossary.md`](glossary.md) — ubiquitous language
- [`decisions/`](decisions/) — architectural decision records
